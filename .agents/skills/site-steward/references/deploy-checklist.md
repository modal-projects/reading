# Deploy checklist

- `uv.lock` exists and matches `pyproject.toml`
- `package-lock.json` exists and matches `package.json`
- `reading site validate` passes
- `npm run build` passes
- Modal credentials are loaded
